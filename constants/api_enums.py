from enum import Enum


class ApiEnums(Enum):
    HEALTH = "/health"
    DEVICES = "/devices"
    DEVICE_REGISTER = "/device/register"
    AUDIO_CHAPTER = "/audio/chapter"
    SEARCH = "/search"
    CHAPTERS = "/chapters"
